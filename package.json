{
  "name": "trading-app",
  "version": "1.0.0",
  "description": "Trading app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd renderer && yarn dev\" \"cd electron && yarn dev\""
  },
  "author": "hoangzzzsss",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1",
    "wait-on": "^7.0.1"
  },
  "devDependencies": {}
}
